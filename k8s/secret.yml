# StructAI – Secret (sensitive values — populate before applying)
#
# In production, use an external secret manager (e.g. AWS Secrets Manager,
# HashiCorp Vault, or Sealed Secrets) rather than plain K8s Secrets.
#
# To create from literal values:
#   kubectl create secret generic structai-secrets \
#     --namespace=structai \
#     --from-literal=OPENAI_API_KEY=sk-... \
#     --from-literal=POSTGRES_USER=ai_user \
#     --from-literal=POSTGRES_PASSWORD=<strong-password>
#
# This file is a template — values below are base64-encoded placeholders.
apiVersion: v1
kind: Secret
metadata:
  name: structai-secrets
  namespace: structai
  labels:
    app.kubernetes.io/name: structai
type: Opaque
data:
  # echo -n "your-openai-api-key" | base64
  OPENAI_API_KEY: PHJlcGxhY2UtbWU+
  # echo -n "ai_user" | base64
  POSTGRES_USER: YWlfdXNlcg==
  # echo -n "change-me-in-production" | base64
  POSTGRES_PASSWORD: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24=
