# StructAI – API Service (ClusterIP)
apiVersion: v1
kind: Service
metadata:
  name: structai-api
  namespace: structai
  labels:
    app.kubernetes.io/name: structai
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
